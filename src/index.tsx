import React, { Component } from 'react';
import UnauthorizedScreens from "./Router";

export default class App extends Component {
  render() {
    return (
      <UnauthorizedScreens />
    );
  }
}
